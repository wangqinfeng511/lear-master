在此环境中：
	要求：
	 	redis不可中断业务，
		redis主从数据要同步。
		keepalived要执行切换。
	在方案中：
		在redis中使用主从同步方式。是在redis.conf中实现。未使用nfs方式。
		vip
		飘移的方式进行主从切换，同时要保证切换后从redis要能从主redis中进行数据的同步。
		：采用方法，每台准备两个redis_conf文件，一个在主模下的，一个在从模式下的。为keepalivd切换时使用。
		##############################################################
第三：redis的状态检查，判断进行切换。
	方法：采用调用redis服务器的tomcat服务器每三分钟执行一次get
	key的请求，如果不成功，发送down到192.168.110.102可飘移的地址
