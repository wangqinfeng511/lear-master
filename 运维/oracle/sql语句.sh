select * from  tables
where	限制条件
distinct  去重
|| 连接运算符。类'str'+'str'
lower() 函数把输出转为小写。
initcap() 首字母大写。
concat() 连接两个结果，可以是字符串也可是字断。
substr() 从一个字符串中获取一个字串，例： select substr('wo shi shen me ?',5,10) 取出5－10的字节。
length() 统计字符串的个数跟py中的len() 一样。
lpad() 查找一个字符串的位置 例：lpad(name,10,'*'),10表示输出结果需要10个字符，＊表示不足10个用＊补充。
rpad() 右补充。
